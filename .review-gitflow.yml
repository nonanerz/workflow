stages:
  - review

workflow:
  rules:
    - when: always

review:
  image: nonanerz/ai-review:gpt-3.5-turbo
  script:
    - python /app/main.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: always
    - when: never
